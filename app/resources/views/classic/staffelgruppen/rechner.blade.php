<!DOCTYPE html><html lang="de"><head><meta charset="UTF-8"><title>Staffelrechner</title><style>body{font:13px Arial;background:#d9d9d9;padding:12px}.window{max-width:1050px;margin:auto;background:#efefef;border:1px solid #888;padding:14px}.grid{display:grid;grid-template-columns:1fr 1.3fr;gap:20px}.field{display:grid;grid-template-columns:130px 1fr 45px;gap:7px;align-items:center;margin:7px 0}input{padding:5px;border:1px solid #999}.button{padding:7px 13px;border:1px solid #777;background:#eee;color:#111;text-decoration:none;cursor:pointer}.tbl{width:100%;border-collapse:collapse}.tbl th,.tbl td{border:1px solid #aaa;padding:5px}.tbl th{background:#ddd}.ok{background:#e8f6e8;border:1px solid #4b8b4b;padding:8px}.err{background:#fee;border:1px solid #b91c1c;padding:8px}</style></head><body>
<x-page-help title="Staffelgruppen">Hier werden Staffelgruppen für Abrechnungsart 1 gepflegt und berechnet. Änderungen an Grenzen oder Preisen beeinflussen spätere Rechnungen. Vor dem Speichern Staffelreihenfolge und Beträge prüfen.</x-page-help>
<div class="window"><h2>Staffelrechner · Staffel {{ $sg->intID }}</h2>@if(session('status'))<div class="ok">{{ session('status') }}</div>@endif @if($errors->any())<div class="err">{{ implode(' · ',$errors->all()) }}</div>@endif<div class="grid"><div><form method="post" action="{{ route('staffelgruppen.rechner.run',$sg->intID) }}" onsubmit="return confirm('Preisstufen jetzt erzeugen? Die neuen Zeilen werden zusätzlich angelegt.');">@csrf
<div class="field"><label>Startwert</label><input type="number" name="startwert" value="{{ old('startwert') }}" required min="0"><span>{{ trim($sg->strAbrechnungseinheit) }}</span></div><div class="field"><label>Endwert</label><input type="number" name="endwert" value="{{ old('endwert') }}" required min="1"><span>{{ trim($sg->strAbrechnungseinheit) }}</span></div><div class="field"><label>Schrittweite</label><input type="number" name="schrittweite" value="{{ old('schrittweite') }}" required min="1"><span>{{ trim($sg->strAbrechnungseinheit) }}</span></div><div class="field"><label>Schrittpreis</label><input type="number" step="0.0001" name="schrittpreis" value="{{ old('schrittpreis') }}" required><span>€</span></div><div class="field"><label>Startpreis</label><input type="number" step="0.0001" name="startpreis" value="{{ old('startpreis') }}" required><span>€</span></div><div class="field"><label>Staffel-ID</label><input value="{{ $sg->intID }}" readonly><span></span></div><p><button class="button">Ausführen</button> <button class="button" type="reset">Eingaben löschen</button> <a class="button" href="{{ route('staffelgruppen.edit',$sg->intID) }}">Schließen</a></p></form><p><small>Wie in Access werden Schrittweite und Schrittpreis vor dem ersten INSERT addiert.</small></p></div><div><table class="tbl"><thead><tr><th>ID</th><th>Staffel-ID</th><th>Menge</th><th>Preis</th></tr></thead><tbody>@foreach($preise as $p)<tr><td>{{ $p->intID }}</td><td>{{ $p->intStaffelgruppeID }}</td><td>{{ $p->intMenge }}</td><td>{{ number_format((float)$p->intvkpreis,4,',','.') }} €</td></tr>@endforeach</tbody></table></div></div></div><script src="{{ asset('js/db-window-manager.js') }}"></script></body></html>

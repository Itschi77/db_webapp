<!DOCTYPE html><html lang="de"><head><meta charset="UTF-8"><title>{{ $tarif ? 'Bandbreiten-Tarif '.$tarif->intID : 'Neuer Bandbreiten-Tarif' }}</title><style>body{font:13px Arial;background:#d9d9d9;padding:12px}.window{max-width:900px;margin:auto;background:#efefef;border:1px solid #888;padding:14px}.top{display:flex;justify-content:space-between;align-items:center}.field{display:grid;grid-template-columns:160px 1fr;gap:8px;margin:8px 0}input{padding:5px;border:1px solid #999}.button{padding:7px 13px;border:1px solid #777;background:#eee;color:#111;text-decoration:none;cursor:pointer}.tbl{width:100%;border-collapse:collapse;margin-top:12px}.tbl th,.tbl td{border:1px solid #aaa;padding:5px}.tbl th{background:#ddd}.ok{background:#e8f6e8;border:1px solid #4b8b4b;padding:8px}.err{background:#fee;border:1px solid #b91c1c;padding:8px}</style></head><body><div class="window"><div class="top"><h2>{{ $tarif ? 'Tarifnummer '.$tarif->intID : 'Neuer Bandbreiten-Tarif' }}</h2><a href="{{ route('frontend.switch','modern') }}">Zum neuen Frontend wechseln →</a></div>@if(session('status'))<div class="ok">{{ session('status') }}</div>@endif @if($errors->any())<div class="err">{{ implode(' · ',$errors->all()) }}</div>@endif<form method="post" action="{{ $tarif ? route('bandbreitentarife.update',$tarif->intID) : route('bandbreitentarife.store') }}">@csrf @if($tarif)@method('PUT')@endif<div class="field"><label>Name des Tarifs</label><input name="strBezeichnung" maxlength="255" required value="{{ old('strBezeichnung',$tarif->strBezeichnung ?? '') }}"></div><p><button class="button">Speichern</button> <a class="button" href="{{ route('bandbreitentarife.index') }}">Schließen</a></p></form>@if($tarif)<h3>Bandbreitenpreise</h3><table class="tbl"><thead><tr><th>ID</th><th>kBit / Sekunde</th><th>Nettopreis</th><th>Aktion</th></tr></thead><tbody>@foreach($preise as $p)@php($fid='preis-'.$p->intID)<form id="{{ $fid }}" method="post" action="{{ route('bandbreitentarife.preise.update',[$tarif->intID,$p->intID]) }}">@csrf @method('PUT')</form><tr><td>{{ $p->intID }}</td><td><input form="{{ $fid }}" type="number" min="0" step="1" name="intMenge" value="{{ $p->intMenge }}" required></td><td><input form="{{ $fid }}" type="number" step="0.0001" name="fVkPreis" value="{{ $p->fVkPreis }}" required></td><td><button form="{{ $fid }}" class="button">Speichern</button></td></tr>@endforeach<form id="preis-neu" method="post" action="{{ route('bandbreitentarife.preise.store',$tarif->intID) }}">@csrf</form><tr><td>neu</td><td><input form="preis-neu" type="number" min="0" step="1" name="intMenge" required></td><td><input form="preis-neu" type="number" step="0.0001" name="fVkPreis" required></td><td><button form="preis-neu" class="button">Preiszeile anlegen</button></td></tr></tbody></table><p><small>„Automatisch berechnen…“ aus Access wird nicht nachgebildet, da am bestätigten Formular keine Ereignisprozedur hinterlegt ist. Löschen ist im Web derzeit nicht freigegeben.</small></p>@endif</div><script src="{{ asset('js/db-window-manager.js') }}"></script></body></html>
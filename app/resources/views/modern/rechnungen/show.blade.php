<!DOCTYPE html><html lang="de"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Rechnung {{ $rechnung->intRechNr }}</title><style>*{box-sizing:border-box}body{margin:0;font:14px Segoe UI,Arial;background:#f4f6f8;color:#1f2937}.page{max-width:1050px;margin:auto;padding:28px}.card{background:#fff;border-radius:12px;box-shadow:0 2px 8px #0001;padding:22px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.field{padding:10px;background:#f8fafc;border-radius:8px}.label{font-size:12px;text-transform:uppercase;color:#64748b;margin-bottom:4px}.button{display:inline-block;padding:9px 13px;border-radius:7px;background:#0369a1;color:#fff;text-decoration:none}.money{text-align:right}</style></head><body><div class="page"><div style="text-align:right"><a href="{{ route('frontend.switch','classic') }}">← Zur klassischen Ansicht</a></div><h1>Rechnung {{ $rechnung->intRechNr }}</h1><div class="card"><div class="grid"><div class="field"><div class="label">Rechnungsdatum</div>{{ date('d.m.Y',strtotime($rechnung->datRechnungsDatum)) }}</div><div class="field"><div class="label">Fällig</div>{{ date('d.m.Y',strtotime($rechnung->datFaelligkeitsDatum)) }}</div><div class="field"><div class="label">Kunde</div>{{ $kunde?->strName ?? $rechnung->strKundenNameAufRechnung }}</div><div class="field"><div class="label">Auftrag</div>{{ $rechnung->intAufNr }}</div><div class="field money"><div class="label">Netto</div>{{ number_format((float)$rechnung->fBetrag,2,',','.') }} €</div><div class="field money"><div class="label">Steuer</div>{{ number_format((float)$rechnung->fSteuer,2,',','.') }} €</div><div class="field money"><div class="label">Gesamt</div><strong>{{ number_format((float)$rechnung->fRechnungsbetrag,2,',','.') }} €</strong></div><div class="field money"><div class="label">Bezahlt</div>{{ number_format((float)$rechnung->fBezahlterBetrag,2,',','.') }} €</div><div class="field"><div class="label">Status</div>{{ $rechnung->boolBezahlt ? 'Bezahlt' : 'Offen' }}{{ $rechnung->datBezahlDatum ? ' am '.date('d.m.Y',strtotime($rechnung->datBezahlDatum)) : '' }}</div><div class="field"><div class="label">Mahnstufe</div>{{ $rechnung->intMahnstufe ?? '-' }}</div></div><h3>Hinweise</h3><p><strong>Auftragsbeschreibung:</strong><br>{{ $rechnung->strKopieAuftragsbeschreibung }}</p><p><strong>Zahlungskommentar:</strong><br>{{ $rechnung->strZahlungskommentar ?: '-' }}</p>@if($rechnung->bolRechnungStrittig)<p><strong>Strittig:</strong> {{ $rechnung->strRechnungStrittigGrund ?: 'Ja' }}</p>@endif<p><a class="button" href="{{ route('rechnungen.index') }}">Zur Übersicht</a>@if($kunde && $auftrag) <a class="button" href="{{ route('kunden.auftraege.show',[$kunde->intID,$auftrag->intAufNr]) }}">Auftrag öffnen</a>@endif</p></div></div><script src="{{ asset('js/db-window-manager.js') }}"></script></body></html>
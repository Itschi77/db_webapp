<!DOCTYPE html><html lang="de"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Netze</title><style>
body{font:14px "Segoe UI",Arial;background:#f4f6f8;color:#1f2937;padding:26px}.page{max-width:1450px;margin:auto}.card{background:#fff;border:1px solid #d1d5db;border-radius:10px;padding:20px;box-shadow:0 4px 14px #0001}.top{display:flex;justify-content:space-between;align-items:center}.button{display:inline-block;padding:8px 14px;border:1px solid #9ca3af;border-radius:7px;background:#fff;color:#111827;text-decoration:none;cursor:pointer}.search{display:flex;gap:10px;align-items:end;margin:15px 0}input{padding:8px;border:1px solid #9ca3af;border-radius:6px}table{width:100%;border-collapse:collapse}th,td{padding:8px;border-bottom:1px solid #e5e7eb;vertical-align:top}th{background:#f9fafb;text-align:left}.muted{color:#6b7280}.pager{display:flex;gap:10px;align-items:center;margin-top:14px}</style></head><body><div class="page"><div class="card"><div class="top"><h1>Netze</h1><a href="{{ route('frontend.switch','classic') }}">← Zur klassischen Ansicht</a></div>
<p><a class="button" href="{{ route('netze.create') }}">Neues Netz</a> <a class="button" href="{{ route('dashboard') }}">Hauptmenü</a></p>
<form class="search" method="get"><label>Suchen<br><input name="q" value="{{ $q }}" size="34" placeholder="ID, Netz, Verwendung, Standort, Rechnungsinfo"></label><button class="button">Suchen</button>@if($q!=='')<a class="button" href="{{ route('netze.index') }}">Filter entfernen</a>@endif</form>
<table><thead><tr><th>ID</th><th>Netz</th><th>Gateway</th><th>Flags</th><th>Verwendung</th><th>Standort</th><th>Rechnungsinfo</th><th></th></tr></thead><tbody>@forelse($netze as $n)<tr><td>{{ $n->intID }}</td><td><strong>{{ trim($n->strNetzwerk) }}/{{ $n->intNetzmaske }}</strong></td><td>{{ $n->intGatewayRouter }}</td><td>KundenNetz: {{ (int)$n->intKundenNetz!==0?'Ja':'Nein' }}<br>Accounting: {{ (int)$n->intAccountingEingerichtet!==0?'Ja':'Nein' }}<br>InUse: {{ (int)$n->intInUse!==0?'Ja':'Nein' }}</td><td>{{ $n->strVerwendung }}@if($n->strBemerkung)<br><span class="muted">{{ $n->strBemerkung }}</span>@endif</td><td>{{ $n->strStandort }}</td><td>{{ $n->strrechnungsinfo }}</td><td><a class="button" href="{{ route('netze.edit',$n->intID) }}">Öffnen</a></td></tr>@empty<tr><td colspan="8">Keine Netze gefunden.</td></tr>@endforelse</tbody></table>
<div class="pager">@if($netze->previousPageUrl())<a class="button" href="{{ $netze->previousPageUrl() }}">← Zurück</a>@endif <span>Seite {{ $netze->currentPage() }} von {{ $netze->lastPage() }} · {{ $netze->total() }} Treffer</span> @if($netze->nextPageUrl())<a class="button" href="{{ $netze->nextPageUrl() }}">Weiter →</a>@endif</div></div></div><script src="{{ asset('js/db-window-manager.js') }}"></script></body></html>

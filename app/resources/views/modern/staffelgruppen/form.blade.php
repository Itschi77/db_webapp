<!DOCTYPE html><html lang="de"><head><meta charset="UTF-8"><title>{{ $sg ? 'Staffelgruppe '.$sg->intID : 'Neue Staffelgruppe' }}</title><style>body{font:14px "Segoe UI",Arial;background:#f4f6f8;color:#1f2937;padding:26px}.page{max-width:1150px;margin:auto}.card{background:#fff;border:1px solid #d1d5db;border-radius:10px;padding:20px;box-shadow:0 4px 14px #0001}.top{display:flex;justify-content:space-between;align-items:center}.field{display:grid;grid-template-columns:190px 1fr;gap:10px;margin:10px 0}input{padding:8px;border:1px solid #9ca3af;border-radius:6px}.button{padding:8px 14px;border:1px solid #9ca3af;border-radius:7px;background:#fff;color:#111827;text-decoration:none;cursor:pointer}.tbl{width:100%;border-collapse:collapse;margin-top:15px}.tbl th,.tbl td{border-bottom:1px solid #e5e7eb;padding:8px}.tbl th{background:#f9fafb}.ok{background:#ecfdf5;border:1px solid #86efac;padding:10px;border-radius:7px}.err{background:#fef2f2;border:1px solid #fca5a5;padding:10px;border-radius:7px}</style></head><body><div class="page"><div class="card"><div class="top"><h1>{{ $sg ? 'Staffelgruppe '.$sg->intID : 'Neue Staffel' }}</h1><a href="{{ route('frontend.switch','classic') }}">← Zur klassischen Ansicht</a></div>
@if(session('status'))<div class="ok">{{ session('status') }}</div>@endif @if($errors->any())<div class="err">{{ implode(' · ',$errors->all()) }}</div>@endif
<form method="post" action="{{ $sg ? route('staffelgruppen.update',$sg->intID) : route('staffelgruppen.store') }}">@csrf @if($sg)@method('PUT')@endif<div class="field"><label>Bezeichnung</label><input name="strBezeichnung" value="{{ old('strBezeichnung',$sg->strBezeichnung ?? '') }}" required maxlength="255"></div><div class="field"><label>Abrechnungseinheit</label><input name="strAbrechnungseinheit" value="{{ old('strAbrechnungseinheit',isset($sg)?trim($sg->strAbrechnungseinheit):'') }}" required maxlength="10"></div><p><button class="button">Speichern</button> <a class="button" href="{{ route('staffelgruppen.index') }}">Zur Liste</a>@if($sg) <a class="button" href="{{ route('staffelgruppen.rechner',$sg->intID) }}">Staffelrechner</a>@endif</p></form>
@if($sg)<h2>Preisstufen</h2><table class="tbl"><thead><tr><th>ID</th><th>Menge</th><th>VK-Preis</th><th>Aktion</th></tr></thead><tbody>@foreach($preise as $p)@php($fid='preis-'.$p->intID)<form id="{{ $fid }}" method="post" action="{{ route('staffelgruppen.preise.update',[$sg->intID,$p->intID]) }}">@csrf @method('PUT')</form><tr><td>{{ $p->intID }}</td><td><input form="{{ $fid }}" type="number" step="1" name="intMenge" value="{{ $p->intMenge }}" required></td><td><input form="{{ $fid }}" type="number" step="0.0001" name="intvkpreis" value="{{ $p->intvkpreis }}" required></td><td><button form="{{ $fid }}" class="button">Speichern</button></td></tr>@endforeach<form id="preis-neu" method="post" action="{{ route('staffelgruppen.preise.store',$sg->intID) }}">@csrf</form><tr><td>neu</td><td><input form="preis-neu" type="number" step="1" min="0" name="intMenge" required></td><td><input form="preis-neu" type="number" step="0.0001" name="intvkpreis" required></td><td><button form="preis-neu" class="button">Preisstufe anlegen</button></td></tr></tbody></table><p><small>Löschen ist im Web bewusst nicht freigegeben.</small></p>@endif</div></div><script src="{{ asset('js/db-window-manager.js') }}"></script></body></html>

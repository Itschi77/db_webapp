<!DOCTYPE html><html lang="de"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Anbindungen</title><style>
body{font:14px "Segoe UI",Arial;background:#f4f6f8;color:#1f2937;padding:26px}.page{max-width:1450px;margin:auto}.card{background:#fff;border:1px solid #d1d5db;border-radius:10px;padding:20px;box-shadow:0 4px 14px #0001}.top{display:flex;justify-content:space-between;align-items:center}.button{display:inline-block;padding:8px 14px;border:1px solid #9ca3af;border-radius:7px;background:#fff;color:#111827;text-decoration:none;cursor:pointer}.search{display:flex;gap:10px;flex-wrap:wrap;align-items:end;margin:15px 0}input,select{padding:8px;border:1px solid #9ca3af;border-radius:6px}table{width:100%;border-collapse:collapse}th,td{padding:8px;border-bottom:1px solid #e5e7eb;vertical-align:top}th{background:#f9fafb;text-align:left}.muted{color:#6b7280}.pager{display:flex;gap:10px;align-items:center;margin-top:14px}</style></head><body><div class="page"><div class="card"><div class="top"><h1>Anbindungen</h1><a href="{{ route('frontend.switch','classic') }}">← Zur klassischen Ansicht</a></div>
<p><a class="button" href="{{ route('anbindungen.create') }}">Neue Anbindung</a> <a class="button" href="{{ route('dashboard') }}">Hauptmenü</a></p>
<form class="search" method="get"><label>Suchen<br><input name="q" value="{{ $q }}" placeholder="ID, Auftrag, Kunde, Rechnungsinfo" size="34"></label><label>Accounting-Art<br><select name="typ"><option value="0">alle</option>@foreach($types as $k=>$label)<option value="{{ $k }}" @selected($type===$k)>{{ $k }} · {{ $label }}</option>@endforeach</select></label><button class="button">Suchen</button></form>
<table><thead><tr><th>ID</th><th>Art</th><th>Referenz</th><th>Technische Information</th><th>Kunde</th><th>Auftrag / Position</th><th>Abrechnung</th><th></th></tr></thead><tbody>@forelse($anbindungen as $a)<tr><td>{{ $a->intID }}</td><td>{{ $types[(int)$a->intTyp] ?? 'Typ '.$a->intTyp }}</td><td>{{ $a->intAnbindungReferenz }}</td><td>@if($a->referenzInfo)<strong>{{ $a->referenzInfo['title'] }}</strong><br><span class="muted">{{ $a->referenzInfo['detail'] }}</span>@else<span class="muted">keine Referenzinformation</span>@endif</td><td>#{{ $a->auftragKID }} · {{ $a->kunde->strName ?? '' }}</td><td>{{ $a->intAufNr }} / {{ $a->intAuftragsPos }}</td><td>{{ $a->boolAbrechenbar ? 'abrechenbar':'nicht abrechenbar' }}<br><span class="muted">{{ date('d.m.Y H:i:s',strtotime($a->dateAbrechenbarStart)) }} bis {{ date('d.m.Y H:i:s',strtotime($a->dateAbrechenbarEnde)) }}</span></td><td><a class="button" href="{{ route('anbindungen.edit',$a->intID) }}">Öffnen</a></td></tr>@empty<tr><td colspan="8">Keine Anbindungen gefunden.</td></tr>@endforelse</tbody></table>
<div class="pager">@if($anbindungen->previousPageUrl())<a class="button" href="{{ $anbindungen->previousPageUrl() }}">← Zurück</a>@endif <span>Seite {{ $anbindungen->currentPage() }} von {{ $anbindungen->lastPage() }} · {{ $anbindungen->total() }} Treffer</span> @if($anbindungen->nextPageUrl())<a class="button" href="{{ $anbindungen->nextPageUrl() }}">Weiter →</a>@endif</div>
</div></div><script src="{{ asset('js/db-window-manager.js') }}"></script></body></html>
